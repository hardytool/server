extends ../components/base.pug

block title
  title #{title} : No active divisions

block content
  div.columns
    div.column
      h3.title.is-3.has-icon
        span.icon.is-medium
          i.fa.fa-exclamation-triangle
        span No active divisions
  div.columns
    div.column
      if error
        span= error
      else
        div There aren't any active divisions available.
        div Ping an admin.
