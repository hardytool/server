version: '3'
services:
  server:
    build: .
    ports:
      - '8080:8080'
    links:
      - 'db'
    restart: 'always'
    environment:
      - 'STEAM_API_KEY'
      - 'HOST'
      - 'PORT'
    network_mode: 'default'

  db:
    image: 'library/postgres:latest'
    restart: 'always'
    environment:
      - 'POSTGRES_USER'
      - 'POSTGRES_PASSWORD'
      - 'PGDATA'
      - 'POSTGRES_DB'
    network_mode: 'default'
