console.log(`
Currently, there are no tests for this project.

This isn't ideal by any means, so to fix this we need to run the server under
docker and use credentials stored within Travis to authenticate.

Admin functionality *will not* be checked in this manner.
`)
